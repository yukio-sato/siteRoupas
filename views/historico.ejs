<!DOCTYPE html>
<html lang="pt-br">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="icon" type="image/png" href="css/media/logo.png">
    <link rel="stylesheet" href="css/geral.css">
    <link rel="stylesheet" href="css/historico.css">

    <!-- FONTE -->
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link
        href="https://fonts.googleapis.com/css2?family=DM+Sans:ital,opsz,wght@0,9..40,100..1000;1,9..40,100..1000&family=DM+Serif+Display:ital@0;1&family=Inter:ital,opsz,wght@0,14..32,100..900;1,14..32,100..900&display=swap"
        rel="stylesheet">

    <title>Detalhes do Produto</title>
</head>

<body>
    <% $logged = 'false'
    if (login != 'false'){
        $logged = login;
    }%>
    <header>
        <div id="h-content">
            <div id="h-logos">
                <img id="h-icon" src="css/media/logo.png" alt="Logo">
                <img id="h-logotxt" src="css/media/logo escrita.png" alt="Ilha das True">
            </div>
            <img id="menu" src="css/media/restore.png" onclick="window.location.href = '#'">
            <img id="menu" src="css/media/instagram2.png" onclick="window.location.href='https://www.instagram.com/ilhadastrue/'">
            <a href="javascript:void(0);" onclick="Menu()">
                <img id="menu2" src="css/media/hmbrgr.png" alt="hmburgr">
            </a>
            <% if ($logged == 'false'){ %>
                <form action="entrar" method="get">
                    <button class="CDbtn"> Entrar </button>
            <% } else { %>
                <form action="/" method="post">
                    <input type="hidden" name="logged" value="false">
                    <button class="CDbtn"> Sair </button>
                </form>
            <% } %>
        </div>

        <div id="topicos">
            <form action="/#Nike" method="post">
                <input type="hidden" name="logged" value="<%= $logged  %>">
                <button>Nike</button>
            </form>
            <form action="/#Adidas" method="post">
                <input type="hidden" name="logged" value="<%= $logged  %>">
                <button>Adidas</button>
            </form>
            <form action="/#Lacoste" method="post">
                <input type="hidden" name="logged" value="<%= $logged  %>">
                <button>Lacoste</button>
            </form>
            <form action="/#VerMais" method="post">
                <input type="hidden" name="logged" value="<%= $logged  %>">
                <button>Ver Mais</button>
            </form>
        </div>
    </header>

    <div id="main-content">
        <form action="/" method="post">
            <input type="hidden" name="logged" value="<%= $logged  %>">
            <button id="back">Voltar para o Inicio</button>
        </form>

        <h2>Peças Visualizadas</h2>
        <p>Mostramos apenas as seis peças mais recentes</p>

        <div id="RoupasSecao">
            <%if (data != 'Vazio'){
                $numberClothe = 0;
                data.forEach((entry)=> {
                if ($numberClothe < 6){ $numberClothe++; %>
                    <form id="Roupas" action="produto" method="post">
                        <img src="<%= entry.url_roupa %>" alt="camiseta da <%= entry.cat_roupa %>" id="RoupaImg">
                        <input type="hidden" name="logged" value="<%= login %>">
                            <input type="hidden" name="viewing" value="true">
                        <input type="hidden" name="hiddenVl" value="<%= entry.nm_roupa %>">
                        <input type="hidden" name="hiddenVl3" value="<%= entry.cat_roupa %>">
                        <%= entry.nm_roupa %>
                        <h3>R$<%= entry.vl_roupa %></h3>
                        <% if (entry.cat_roupa != 'Nike' && entry.cat_roupa != 'Adidas' && entry.cat_roupa != 'Lacoste'){ %>
                            <button class="BuyMore">Ver Produto</button>
                        <% } else { %>
                            <button class="Buy<%= entry.cat_roupa %>">Ver Produto</button>
                        <% } %>
                    </form>
                    <% } %>
                <% }) %>
            <% } %>
        </div>
    </div>

    <div id="footer">
        Site desenvolvido por<a href="https://github.com/niButera">Nicolas Butera</a>e<a
            href="https://github.com/yukio-sato">Yukio Sato</a>
    </div>

    <script src="js/hamburguer.js"></script>
</body>

</html>